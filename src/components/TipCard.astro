---
interface Props {
  number: number;
  title: string;
  description: string;
}

const { number, title, description } = Astro.props;
---

<article class="tip-card">
  <div class="tip-number">{number}</div>
  <h3>{title}</h3>
  <p>{description}</p>
</article>
